/**
 * Zustand stores index
 * 
 * Re-exports all Zustand stores for easy importing throughout the application.
 * 
 * @example
 * ```typescript
 * import { useExampleStore } from '@/stores'
 * ```
 */

export { useExampleStore } from './example.store'