# 신뢰할 수 있는 패키지들의 build scripts 허용
# Prisma 관련 패키지들 (데이터베이스 클라이언트 생성용)
@prisma/client=allow-scripts
@prisma/engines=allow-scripts
prisma=allow-scripts

# 필수 빌드 도구들
esbuild=allow-scripts
sharp=allow-scripts

# 기타 필요한 패키지들
core-js-pure=allow-scripts
unrs-resolver=allow-scripts

# 모든 scripts를 허용하려면 (보안상 권장하지 않음)
# enable-pre-post-scripts=true